# input: df: pd.DataFrame, col: list[str]
# Tổ chức thành class
# Hàm in matrix chuẩn hoá
# Hàm trọng số cuối của các col (mean của hàng) trả về dạng %
# Hàm hai hàm minmax, maxmin
# Hàm in ra solution chọn target cho từng col